# 🔄 วิธีกลับไปใช้แบบเดิม (Maximum Performance)

หากต้องการความเร็วและคุณภาพสูงสุด แต่ยอมจ่าย **Railway 8 GB RAM plan (~$30-40/เดือน)**

---

## 📋 เปรียบเทียบ

| การตั้งค่า | RAM ใช้ | ความเร็ว | คุณภาพ | ค่าใช้จ่าย |
|-----------|---------|----------|--------|-----------|
| **แบบ Optimized** (ตอนนี้) | ~2 GB | ปกติ | ดี | $20/เดือน |
| **แบบเดิม** (Maximum) | ~5-6 GB | เร็วกว่า 30-50% | ดีที่สุด | $30-40/เดือน |

---

## 🚀 วิธีเปลี่ยนกลับ (3 ขั้นตอน)

### **ขั้นที่ 1: แก้ไข `config.py`**

```python
# บรรทัด 28 - เพิ่มจำนวนเฟรม
VIDEO_MAX_FRAMES: int = 1000  # เปลี่ยนจาก 250 → 1000

# บรรทัด 55 - เปลี่ยนกลับเป็น buffalo_l (model ใหญ่กว่า แม่นกว่า)
FACE_DETECTION_MODEL: str = "buffalo_l"  # เปลี่ยนจาก buffalo_s → buffalo_l
```

---

### **ขั้นที่ 2: แก้ไข `requirements.txt`**

**เปิดใช้ DeepFace + TensorFlow:**

```python
# บรรทัด 16-19 - ลบ # ออก
# DeepFace for Emotion Detection
deepface==0.0.95
tf-keras==2.20.1
tensorflow==2.20.0
```

**ก่อน (ตอนนี้):**
```
# DeepFace for Emotion Detection (DISABLED to save RAM ~1.5-2 GB)
# deepface==0.0.95
# tf-keras==2.20.1
# tensorflow==2.20.0
```

**หลัง (แบบเดิม):**
```
# DeepFace for Emotion Detection
deepface==0.0.95
tf-keras==2.20.1
tensorflow==2.20.0
```

---

### **ขั้นที่ 3: Commit และ Push**

```bash
# แก้ไข config.py และ requirements.txt ตามด้านบน แล้วรัน:

git add config.py requirements.txt

git commit -m "Restore original settings for maximum performance

- VIDEO_MAX_FRAMES: 250 → 1000
- FACE_DETECTION_MODEL: buffalo_s → buffalo_l
- Enable DeepFace/TensorFlow

Requires Railway 8 GB RAM plan"

git push origin main
```

---

## ⚙️ อัพเกรด Railway Plan

**ก่อน Deploy บน Railway:**

1. ไปที่ **Railway Dashboard**
2. เลือก Project **yt01**
3. คลิก **Settings** → **Resources**
4. เปลี่ยนเป็น:
   - **RAM: 8 GB** (จาก 4 GB)
   - **CPU: 8 vCPU** (เหมือนเดิม)
5. Save

**ราคาจะเปลี่ยนเป็น ~$30-40/เดือน**

---

## 📊 ประสิทธิภาพที่คาดหวัง (แบบเดิม)

### **RAM Usage:**
```
TensorFlow + DeepFace:    1.5-2 GB
InsightFace buffalo_l:    ~600 MB
OpenCV + libs:            ~400 MB
Video frames (1000):      ~2 GB (peak)
Python + FastAPI:         ~300 MB
Working memory:           ~500 MB
--------------------------------
Total Peak:               5-6 GB
```

### **Processing Speed:**
```
1000 เฟรม vs 250 เฟรม
- ดึงเฟรม:        เร็วกว่า ~20-30%
- วิเคราะห์หน้า:   เร็วกว่า ~40-50%
- มีตัวเลือกมากกว่า: 4x
- คุณภาพ:         ดีที่สุด!
```

### **เวลารวม:**
```
แบบ Optimized: ~2-3 นาที
แบบเดิม:       ~5-7 นาที (แต่คุณภาพดีกว่า!)
```

---

## 🔙 วิธีกลับมาใช้แบบ Optimized (ประหยัด)

ถ้าลองแล้วไม่พอใจ หรืออยากประหยัดเงิน:

### **ขั้นที่ 1: แก้ไข `config.py`**
```python
VIDEO_MAX_FRAMES: int = 250  # ลดลง
FACE_DETECTION_MODEL: str = "buffalo_s"  # เล็กกว่า
```

### **ขั้นที่ 2: แก้ไข `requirements.txt`**
```python
# DeepFace for Emotion Detection (DISABLED to save RAM ~1.5-2 GB)
# deepface==0.0.95
# tf-keras==2.20.1
# tensorflow==2.20.0
```

### **ขั้นที่ 3: Commit และ Push**
```bash
git add config.py requirements.txt
git commit -m "Back to optimized settings (4 GB RAM)"
git push origin main
```

### **ขั้นที่ 4: ลด Railway Plan กลับเป็น 4 GB**

---

## 💡 ตัวเลือกกลางๆ (แนะนำ!)

หากต้องการสมดุลระหว่างความเร็ว คุณภาพ และราคา:

### **Medium Performance Configuration:**

**`config.py`:**
```python
VIDEO_MAX_FRAMES: int = 400  # กลางๆ (ระหว่าง 250-1000)
FACE_DETECTION_MODEL: str = "buffalo_s"  # เล็ก เร็ว
```

**`requirements.txt`:**
```python
# ปิด DeepFace (ไม่จำเป็น)
# deepface==0.0.95
# tf-keras==2.20.1
# tensorflow==2.20.0
```

**Railway Plan:**
```
RAM: 4 GB  ($20/เดือน)
CPU: 8 vCPU
```

**ผลลัพธ์:**
```
RAM ใช้:      ~2.5-3 GB
เวลา:         ~3-4 นาที
คุณภาพ:       ดีมาก!
ราคา:         $20/เดือน ✅
```

---

## 🎯 สรุป Quick Reference

| ต้องการ | VIDEO_MAX_FRAMES | FACE_MODEL | DeepFace | RAM Plan | ราคา |
|---------|-----------------|------------|----------|----------|------|
| **ประหยัดสุด** | 250 | buffalo_s | ❌ ปิด | 4 GB | $20 |
| **สมดุล** ⭐ | 400 | buffalo_s | ❌ ปิด | 4 GB | $20 |
| **เร็วสุด** | 1000 | buffalo_l | ✅ เปิด | 8 GB | $30-40 |

---

## 📝 Notes

1. **buffalo_l vs buffalo_s:**
   - buffalo_l: ใหญ่กว่า (600 MB), แม่นกว่า ~2-3%
   - buffalo_s: เล็กกว่า (300 MB), เร็วกว่า ~30%

2. **DeepFace/TensorFlow:**
   - กิน RAM มาก (1.5-2 GB)
   - ใช้สำหรับ emotion detection
   - ถ้า `WEIGHT_EMOTION = 0.0` ก็ไม่จำเป็นต้องเปิด

3. **VIDEO_MAX_FRAMES:**
   - มากกว่า = ตัวเลือกมากกว่า = คุณภาพดีกว่า
   - น้อยกว่า = เร็วกว่า = ใช้ RAM น้อยกว่า

4. **Railway Auto-Deploy:**
   - Push ขึ้น GitHub แล้ว Railway จะ auto-deploy ใน 2-3 นาที
   - ดูผลที่ Railway Dashboard → Deployments

---

## 🆘 ถ้ามีปัญหา

### **Out of Memory:**
- ลด `VIDEO_MAX_FRAMES` ลง
- เปลี่ยนเป็น `buffalo_s`
- ปิด DeepFace
- อัพเกรด Railway เป็น 8 GB

### **Slow Processing:**
- เพิ่ม `VIDEO_MAX_FRAMES`
- ใช้ `buffalo_l` (แม่นกว่าแต่ช้ากว่า)

### **Poor Quality:**
- เพิ่ม `VIDEO_MAX_FRAMES` → 400-600
- ตรวจสอบ video input quality

---

**เก็บไฟล์นี้ไว้! ใช้เปลี่ยนกลับไปมาได้ตลอด** 🔄
